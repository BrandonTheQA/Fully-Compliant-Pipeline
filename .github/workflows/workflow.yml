name: Azure Function Deployment Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  discover-apps:
    name: Discover Function Apps
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Discover function apps
        id: set-matrix
        run: |
          apps=$(find api/functionapp -mindepth 1 -maxdepth 1 -type d -exec basename {} \; | jq -R -s -c 'split("\n")[:-1]')
          matrix=$(echo $apps | jq -c '{include: [.[] | {
            app: .,
            "working-dir": ("./api/functionapp/" + .),
            "function-app-name-dev": ("joaz-func-" + . + "-9021-dev"),
            "function-app-name-test": ("joaz-func-" + . + "-9021-test"),
            "function-app-name-stage": ("joaz-func-" + . + "-9021-stage"),
            "function-app-name-prod": ("joaz-func-" + . + "-9021-prod"),
            "app-service-plan-dev": "asp-joaz-func-helloworld-9021-dev",
            "app-service-plan-test": "asp-joaz-func-helloworld-9021-test",
            "app-service-plan-stage": "asp-joaz-func-helloworld-9021-stage",
            "app-service-plan-prod": "asp-joaz-func-helloworld-9021-prod",
            collection: ("postman/" + (. | 
              if . == "helloworld" then "HelloWorld"
              elif . == "helloworld2" then "HelloWorld2" 
              else (. | ascii_upcase[:1] + .[1:])
              end) + "Function.postman_collection.json")
          }]}')
          echo "matrix=$matrix" >> $GITHUB_OUTPUT

  build:
    name: Build Azure Function (${{ matrix.app }})
    runs-on: ubuntu-latest
    needs: discover-apps
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Build and Test Azure Function
        working-directory: ${{ matrix.working-dir }}
        run: |
          mvn clean compile test package
      - name: Set Dist Version
        run: |
          BUILD_NUMBER="${{ github.run_id }}-${{ github.run_number }}-${{ github.run_attempt }}"
          echo "${BUILD_NUMBER}"
          echo "DIST_VERSION=${BUILD_NUMBER}" >> $GITHUB_ENV

  build_ui:
    name: Build UI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: ui/package-lock.json
      - name: Install dependencies
        working-directory: ./ui
        run: npm ci --legacy-peer-deps
      - name: Run tests
        working-directory: ./ui
        run: npm test -- --watchAll=false
      - name: Build UI
        working-directory: ./ui
        env:
          VITE_USER_API_URL: https://joaz-func-user-9021-test.azurewebsites.net/api
          VITE_PRODUCT_API_URL: https://joaz-func-product-9021-test.azurewebsites.net/api
          VITE_ORDER_API_URL: https://joaz-func-order-9021-test.azurewebsites.net/api
        run: npm run build
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ui-build
          path: ui/dist
          retention-days: 7

  # security_scans:
  #   name: Security Scans
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v3
  #     - name: Set up Java 11
  #       uses: actions/setup-java@v3
  #       with:
  #         java-version: '11'
  #         distribution: 'temurin'
  #     - name: Run security scan
  #       working-directory: ./api/functionapp/helloworld
  #       env:
  #         NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
  #       run: |
  #         if [ -n "$NVD_API_KEY" ]; then
  #           mvn org.owasp:dependency-check-maven:check \
  #             -Dnvd.api.key=$NVD_API_KEY
  #         else
  #           echo "NVD_API_KEY not set, running without API key (may be slower)"
  #         fi
  #     - name: Run security scan for helloworld2
  #       working-directory: ./api/functionapp/helloworld2
  #       env:
  #         NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
  #       run: |
  #         if [ -n "$NVD_API_KEY" ]; then
  #           mvn org.owasp:dependency-check-maven:check \
  #             -Dnvd.api.key=$NVD_API_KEY
  #         else
  #           echo "NVD_API_KEY not set, running without API key (may be slower)"
  #         fi
  analyze:
    name: Analyze (${{ matrix.language }})
    # Runner size impacts CodeQL analysis time. To learn more, please see:
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners (GitHub.com only)
    # Consider using larger runners or machines with greater resources for possible analysis time improvements.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      # required for all workflows
      security-events: write

      # required to fetch internal or private CodeQL packs
      packages: read

      # only required for workflows in private repositories
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: actions
          build-mode: none
        - language: java-kotlin
          build-mode: none # This mode only analyzes Java. Set this to 'autobuild' or 'manual' to analyze Kotlin too.
        # CodeQL supports the following values keywords for 'language': 'actions', 'c-cpp', 'csharp', 'go', 'java-kotlin', 'javascript-typescript', 'python', 'ruby', 'rust', 'swift'
        # Use `c-cpp` to analyze code written in C, C++ or both
        # Use 'java-kotlin' to analyze code written in Java, Kotlin or both
        # Use 'javascript-typescript' to analyze code written in JavaScript, TypeScript or both
        # To learn more about changing the languages that are analyzed or customizing the build mode for your analysis,
        # see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning.
        # If you are analyzing a compiled language, you can modify the 'build-mode' for that language to customize how
        # your codebase is analyzed, see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/codeql-code-scanning-for-compiled-languages
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Add any setup steps before running the `github/codeql-action/init` action.
    # This includes steps like installing compilers or runtimes (`actions/setup-node`
    # or others). This is typically only required for manual builds.
    # - name: Setup runtime (example)
    #   uses: actions/setup-example@v1

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.

        # For more details on CodeQL's query packs, refer to: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality

    # If the analyze step fails for one of the languages you are analyzing with
    # "We were unable to automatically build your code", modify the matrix above
    # to set the build mode to "manual" for that language. Then modify this step
    # to build your code.
    # ℹ️ Command-line programs to run using the OS shell.
    # 📚 See https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#jobsjob_idstepsrun
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo 'If you are using a "manual" build mode for one or more of the' \
          'languages you are analyzing, replace this with the commands to build' \
          'your code, for example:'
        echo '  make bootstrap'
        echo '  make release'
        exit 1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"

  verify_toggles:
    name: Verify Toggles
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Verify feature toggles
        run: |
          echo "Verifying feature toggles configuration..."
          # Add feature toggle verification logic here

  create_release_notes:
    name: Create Release Notes
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Generate release notes
        run: |
          echo "Generating release notes for Azure Function deployment..."
          # Add release notes generation logic here

  deploy_dev:
    name: Deploy to Dev (${{ matrix.app }})
    runs-on: ubuntu-latest
    needs: 
      - discover-apps
      - build
      - verify_toggles
      - analyze
      # - security_scans  # Commented out for faster pipeline
    environment: 
      name: Dev
      url: https://joaz-func-helloworld-9021-dev.azurewebsites.net
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Azure CLI script
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az account show
      - name: Deploy Function App
        working-directory: ${{ matrix.working-dir }}
        run: |
          mvn -DfunctionAppName=${{ matrix.function-app-name-dev }} \
          -DappName=${{ matrix.function-app-name-dev }} \
          -DappServicePlanName=${{ matrix.app-service-plan-dev }} \
          -DresourceGroup=brandon \
          -Dregion=westus3 \
          clean package azure-functions:deploy

  deploy_ui_dev:
    name: Deploy UI to Dev
    runs-on: ubuntu-latest
    needs:
      - build_ui
      - verify_toggles
      - analyze
    environment:
      name: Dev
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Create UI App Service if it doesn't exist
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            # Check if the app service already exists
            if ! az webapp show --name joaz-ui-9021-dev --resource-group brandon &>/dev/null; then
              echo "Creating UI App Service joaz-ui-9021-dev..."
              az webapp create \
                --name joaz-ui-9021-dev \
                --resource-group brandon \
                --plan asp-joaz-func-helloworld-9021-dev \
                --runtime "NODE|18LTS"
              
              # Configure the webapp for static site hosting
              az webapp config set \
                --name joaz-ui-9021-dev \
                --resource-group brandon \
                --always-on true
              
              # Set the startup command
              az webapp config set \
                --name joaz-ui-9021-dev \
                --resource-group brandon \
                --startup-file "npm install -g serve && serve -s . -p 8080"
            else
              echo "UI App Service joaz-ui-9021-dev already exists, skipping creation."
            fi
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        working-directory: ./ui
        run: npm ci --legacy-peer-deps
      - name: Build UI for Dev environment
        working-directory: ./ui
        env:
          VITE_USER_API_URL: https://joaz-func-user-9021-dev.azurewebsites.net/api
          VITE_PRODUCT_API_URL: https://joaz-func-product-9021-dev.azurewebsites.net/api
          VITE_ORDER_API_URL: https://joaz-func-order-9021-dev.azurewebsites.net/api
        run: npm run build
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'joaz-ui-9021-dev'
          package: ./ui/dist
          startup-command: 'npm install -g serve && serve -s . -p 8080'
      - name: Configure App Settings
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az webapp config appsettings set --name joaz-ui-9021-dev --resource-group brandon --settings \
              VITE_USER_API_URL=https://joaz-func-user-9021-dev.azurewebsites.net/api \
              VITE_PRODUCT_API_URL=https://joaz-func-product-9021-dev.azurewebsites.net/api \
              VITE_ORDER_API_URL=https://joaz-func-order-9021-dev.azurewebsites.net/api

  smoke_test_dev:
    name: Smoke Test for Dev (${{ matrix.app }})
    needs: 
      - discover-apps
      - deploy_dev
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Health Check
        run: |
          echo "Running health check for ${{ matrix.app }} in Dev environment..."
          
          # Retry logic for health check
          max_attempts=3
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Attempt $attempt of $max_attempts"
            
            # Call health endpoint and check response
            response=$(curl -s -w "\n%{http_code}" "https://${{ matrix.function-app-name-dev }}.azurewebsites.net/api/health")
            http_code=$(echo "$response" | tail -n1)
            body=$(echo "$response" | head -n -1)
            
            echo "HTTP Status: $http_code"
            echo "Response: $body"
            
            # Check if HTTP status is 200 and response contains "UP" status (Spring Boot Actuator format)
            if [ "$http_code" = "200" ] && echo "$body" | grep -q '"status":"UP"'; then
              echo "Health check passed for ${{ matrix.app }} in Dev environment"
              exit 0
            else
              echo "Health check failed for ${{ matrix.app }} in Dev environment (attempt $attempt)"
              if [ $attempt -lt $max_attempts ]; then
                echo "Retrying in 10 seconds..."
                sleep 10
              fi
            fi
            
            attempt=$((attempt + 1))
          done
          
          echo "Health check failed after $max_attempts attempts"
          exit 1

  integration_tests_dev:
    name: Integration Tests (Dev)
    needs: 
      - discover-apps
      - smoke_test_dev
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Newman
        run: npm install -g newman
      - name: Run Postman Integration Tests (Dev)
        run: |
          echo "Running Postman integration tests for Dev environment..."
          
          # Generate unique email to avoid conflicts
          TIMESTAMP=$(date +%s)
          UNIQUE_EMAIL="john.doe.${TIMESTAMP}@example.com"
          
          # Run Postman collection with environment variables for Dev environment
          newman run postman/IntegrationTest.postman_collection.json \
            --env-var "userBaseUrl=https://joaz-func-user-9021-dev.azurewebsites.net/api" \
            --env-var "productBaseUrl=https://joaz-func-product-9021-dev.azurewebsites.net/api" \
            --env-var "orderBaseUrl=https://joaz-func-order-9021-dev.azurewebsites.net/api" \
            --env-var "userEmail=${UNIQUE_EMAIL}" \
            --env-var "userName=John Doe" \
            --env-var "userPassword=SecurePassword123" \
            --env-var "product1Name=Laptop" \
            --env-var "product1Description=High-performance laptop" \
            --env-var "product1Price=999.99" \
            --env-var "product1Quantity=10" \
            --env-var "product1Category=Electronics" \
            --env-var "product1OrderQuantity=1" \
            --env-var "product2Name=Mouse" \
            --env-var "product2Description=Wireless mouse" \
            --env-var "product2Price=29.99" \
            --env-var "product2Quantity=50" \
            --env-var "product2Category=Electronics" \
            --env-var "product2OrderQuantity=2" \
            --env-var "product3Name=Keyboard" \
            --env-var "product3Description=Mechanical keyboard" \
            --env-var "product3Price=79.99" \
            --env-var "product3Quantity=25" \
            --env-var "product3Category=Electronics" \
            --env-var "product3OrderQuantity=1" \
            --reporters cli,json \
            --reporter-json-export postman-test-results-dev.json
          
          if [ $? -ne 0 ]; then
            echo "Postman integration tests failed for Dev environment"
            exit 1
          fi
          
          echo "Postman integration tests completed successfully for Dev environment"

  deploy_test:
    name: Deploy to Test (${{ matrix.app }})
    needs: 
      - discover-apps
      - integration_tests_dev
    runs-on: ubuntu-latest
    environment: 
      name: Test
      url: https://joaz-func-helloworld-9021-test.azurewebsites.net
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Deploy Function App
        working-directory: ${{ matrix.working-dir }}
        run: |
          mvn -DfunctionAppName=${{ matrix.function-app-name-test }} \
          -DappName=${{ matrix.function-app-name-test }} \
          -DappServicePlanName=${{ matrix.app-service-plan-test }} \
          -DresourceGroup=brandon-test \
          -Dregion=eastus2 \
          clean package azure-functions:deploy

  deploy_ui_test:
    name: Deploy UI to Test
    runs-on: ubuntu-latest
    needs:
      - build_ui
      - integration_tests_dev
    environment:
      name: Test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Create UI App Service if it doesn't exist
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            # Check if the app service already exists
            if ! az webapp show --name joaz-ui-9021-test --resource-group brandon-test &>/dev/null; then
              echo "Creating UI App Service joaz-ui-9021-test..."
              az webapp create \
                --name joaz-ui-9021-test \
                --resource-group brandon-test \
                --plan asp-joaz-func-helloworld-9021-test \
                --runtime "NODE|18LTS"
              
              # Configure the webapp for static site hosting
              az webapp config set \
                --name joaz-ui-9021-test \
                --resource-group brandon-test \
                --always-on true
              
              # Set the startup command
              az webapp config set \
                --name joaz-ui-9021-test \
                --resource-group brandon-test \
                --startup-file "npm install -g serve && serve -s . -p 8080"
            else
              echo "UI App Service joaz-ui-9021-test already exists, skipping creation."
            fi
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        working-directory: ./ui
        run: npm ci --legacy-peer-deps
      - name: Build UI for Test environment
        working-directory: ./ui
        env:
          VITE_USER_API_URL: https://joaz-func-user-9021-test.azurewebsites.net/api
          VITE_PRODUCT_API_URL: https://joaz-func-product-9021-test.azurewebsites.net/api
          VITE_ORDER_API_URL: https://joaz-func-order-9021-test.azurewebsites.net/api
        run: npm run build
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'joaz-ui-9021-test'
          package: ./ui/dist
          startup-command: 'npm install -g serve && serve -s . -p 8080'
      - name: Configure App Settings
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az webapp config appsettings set --name joaz-ui-9021-test --resource-group brandon-test --settings \
              VITE_USER_API_URL=https://joaz-func-user-9021-test.azurewebsites.net/api \
              VITE_PRODUCT_API_URL=https://joaz-func-product-9021-test.azurewebsites.net/api \
              VITE_ORDER_API_URL=https://joaz-func-order-9021-test.azurewebsites.net/api

  smoke_test_test:
    name: Smoke Test for Test (${{ matrix.app }})
    needs: 
      - discover-apps
      - deploy_test
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Health Check
        run: |
          echo "Running health check for ${{ matrix.app }} in Test environment..."
          
          # Retry logic for health check
          max_attempts=3
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Attempt $attempt of $max_attempts"
            
            # Call health endpoint and check response
            response=$(curl -s -w "\n%{http_code}" "https://${{ matrix.function-app-name-test }}.azurewebsites.net/api/health")
            http_code=$(echo "$response" | tail -n1)
            body=$(echo "$response" | head -n -1)
            
            echo "HTTP Status: $http_code"
            echo "Response: $body"
            
            # Check if HTTP status is 200 and response contains "UP" status (Spring Boot Actuator format)
            if [ "$http_code" = "200" ] && echo "$body" | grep -q '"status":"UP"'; then
              echo "Health check passed for ${{ matrix.app }} in Test environment"
              exit 0
            else
              echo "Health check failed for ${{ matrix.app }} in Test environment (attempt $attempt)"
              if [ $attempt -lt $max_attempts ]; then
                echo "Retrying in 10 seconds..."
                sleep 10
              fi
            fi
            
            attempt=$((attempt + 1))
          done
          
          echo "Health check failed after $max_attempts attempts"
          exit 1

  integration_tests:
    name: Integration Tests
    needs: 
      - discover-apps
      - smoke_test_test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Newman
        run: npm install -g newman
      - name: Run Postman Integration Tests
        run: |
          echo "Running Postman integration tests..."
          
          # Generate unique email to avoid conflicts
          TIMESTAMP=$(date +%s)
          UNIQUE_EMAIL="john.doe.${TIMESTAMP}@example.com"
          
          # Run Postman collection with environment variables
          newman run postman/IntegrationTest.postman_collection.json \
            --env-var "userBaseUrl=https://joaz-func-user-9021-test.azurewebsites.net/api" \
            --env-var "productBaseUrl=https://joaz-func-product-9021-test.azurewebsites.net/api" \
            --env-var "orderBaseUrl=https://joaz-func-order-9021-test.azurewebsites.net/api" \
            --env-var "userEmail=${UNIQUE_EMAIL}" \
            --env-var "userName=John Doe" \
            --env-var "userPassword=SecurePassword123" \
            --env-var "product1Name=Laptop" \
            --env-var "product1Description=High-performance laptop" \
            --env-var "product1Price=999.99" \
            --env-var "product1Quantity=10" \
            --env-var "product1Category=Electronics" \
            --env-var "product1OrderQuantity=1" \
            --env-var "product2Name=Mouse" \
            --env-var "product2Description=Wireless mouse" \
            --env-var "product2Price=29.99" \
            --env-var "product2Quantity=50" \
            --env-var "product2Category=Electronics" \
            --env-var "product2OrderQuantity=2" \
            --env-var "product3Name=Keyboard" \
            --env-var "product3Description=Mechanical keyboard" \
            --env-var "product3Price=79.99" \
            --env-var "product3Quantity=25" \
            --env-var "product3Category=Electronics" \
            --env-var "product3OrderQuantity=1" \
            --reporters cli,json \
            --reporter-json-export postman-test-results.json
          
          if [ $? -ne 0 ]; then
            echo "Postman integration tests failed"
            exit 1
          fi
          
          echo "Postman integration tests completed successfully"

  deploy_stage:
    name: Deploy to Stage (${{ matrix.app }})
    needs: 
      - discover-apps
      - integration_tests
    runs-on: ubuntu-latest
    environment: 
      name: Stage
      url: https://joaz-func-helloworld-9021-stage.azurewebsites.net
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Deploy Function App
        working-directory: ${{ matrix.working-dir }}
        run: |
          mvn -DfunctionAppName=${{ matrix.function-app-name-stage }} \
          -DappName=${{ matrix.function-app-name-stage }} \
          -DappServicePlanName=${{ matrix.app-service-plan-stage }} \
          -DresourceGroup=brandon-stage \
          -Dregion=eastus2 \
          clean package azure-functions:deploy

  deploy_ui_stage:
    name: Deploy UI to Stage
    runs-on: ubuntu-latest
    needs:
      - build_ui
      - integration_tests
    environment:
      name: Stage
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: ui-build
          path: ui/dist
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Create UI App Service if it doesn't exist
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            # Check if the app service already exists
            if ! az webapp show --name joaz-ui-9021-stage --resource-group brandon-stage &>/dev/null; then
              echo "Creating UI App Service joaz-ui-9021-stage..."
              az webapp create \
                --name joaz-ui-9021-stage \
                --resource-group brandon-stage \
                --plan asp-joaz-func-helloworld-9021-stage \
                --runtime "NODE|18LTS"
              
              # Configure the webapp for static site hosting
              az webapp config set \
                --name joaz-ui-9021-stage \
                --resource-group brandon-stage \
                --always-on true
              
              # Set the startup command
              az webapp config set \
                --name joaz-ui-9021-stage \
                --resource-group brandon-stage \
                --startup-file "npm install -g serve && serve -s . -p 8080"
            else
              echo "UI App Service joaz-ui-9021-stage already exists, skipping creation."
            fi
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        working-directory: ./ui
        run: npm ci --legacy-peer-deps
      - name: Build UI for Stage environment
        working-directory: ./ui
        env:
          VITE_USER_API_URL: https://joaz-func-user-9021-stage.azurewebsites.net/api
          VITE_PRODUCT_API_URL: https://joaz-func-product-9021-stage.azurewebsites.net/api
          VITE_ORDER_API_URL: https://joaz-func-order-9021-stage.azurewebsites.net/api
        run: npm run build
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'joaz-ui-9021-stage'
          package: ./ui/dist
          startup-command: 'npm install -g serve && serve -s . -p 8080'
      - name: Configure App Settings
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az webapp config appsettings set --name joaz-ui-9021-stage --resource-group brandon-stage --settings \
              VITE_USER_API_URL=https://joaz-func-user-9021-stage.azurewebsites.net/api \
              VITE_PRODUCT_API_URL=https://joaz-func-product-9021-stage.azurewebsites.net/api \
              VITE_ORDER_API_URL=https://joaz-func-order-9021-stage.azurewebsites.net/api

  smoke_test_stage:
    name: Smoke Test for Stage (${{ matrix.app }})
    needs: 
      - discover-apps
      - deploy_stage
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Health Check
        run: |
          echo "Running health check for ${{ matrix.app }} in Stage environment..."
          
          # Retry logic for health check
          max_attempts=3
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Attempt $attempt of $max_attempts"
            
            # Call health endpoint and check response
            response=$(curl -s -w "\n%{http_code}" "https://${{ matrix.function-app-name-stage }}.azurewebsites.net/api/health")
            http_code=$(echo "$response" | tail -n1)
            body=$(echo "$response" | head -n -1)
            
            echo "HTTP Status: $http_code"
            echo "Response: $body"
            
            # Check if HTTP status is 200 and response contains "UP" status (Spring Boot Actuator format)
            if [ "$http_code" = "200" ] && echo "$body" | grep -q '"status":"UP"'; then
              echo "Health check passed for ${{ matrix.app }} in Stage environment"
              exit 0
            else
              echo "Health check failed for ${{ matrix.app }} in Stage environment (attempt $attempt)"
              if [ $attempt -lt $max_attempts ]; then
                echo "Retrying in 10 seconds..."
                sleep 10
              fi
            fi
            
            attempt=$((attempt + 1))
          done
          
          echo "Health check failed after $max_attempts attempts"
          exit 1

  performance_test:
    name: Performance tests
    needs: deploy_stage
    runs-on: ubuntu-latest
    steps:
      - name: Run Performance Tests for Azure Function
        run: |
          echo "Running performance tests for Azure Function..."
          # Add performance test commands here

  qa_certified:
    name: Certify QA Regression Testing
    needs: 
      - smoke_test_stage
      - performance_test
    runs-on: ubuntu-latest
    steps:
      - name: Manual QA Certification
        run: |
          echo "QA certification required - please check the workflow run and approve manually"
          echo "All quality testing has been completed successfully"
          # Manual approval step - in a real scenario, this would use a manual approval action
          # For now, we'll just log the requirement

  verify_jira:
    name: Verify Jira Stories closed
    needs: performance_test
    runs-on: ubuntu-latest
    steps:
      - name: Verify Jira Stories
        run: |
          echo "Verifying Jira stories are closed..."
          # Add Jira verification logic here

  create_cr:
    name: Create Change Request
    needs: 
      - verify_jira
      - qa_certified
    runs-on: ubuntu-latest
    steps:
      - name: Create Change Request
        run: |
          echo "Creating change request for Azure Function deployment..."
          # Add change request creation logic here

  stage_production_build:
    name: Stage Production Build
    needs: 
      - create_cr
    runs-on: ubuntu-latest
    steps:
      - name: Manual Production Staging Approval
        run: |
          echo "Production staging approval required - please check the workflow run and approve manually"
          echo "All pre-production checks have been completed successfully"
          # Manual approval step - in a real scenario, this would use a manual approval action
          # For now, we'll just log the requirement

  deploy_production:
    name: Deploy to Production (${{ matrix.app }})
    needs: 
      - discover-apps
      - stage_production_build
    runs-on: ubuntu-latest
    environment: 
      name: Production
      url: https://joaz-func-helloworld-9021-prod.azurewebsites.net
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up Java 11
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Deploy Function App
        working-directory: ${{ matrix.working-dir }}
        run: |
          mvn -DfunctionAppName=${{ matrix.function-app-name-prod }} \
          -DappName=${{ matrix.function-app-name-prod }} \
          -DappServicePlanName=${{ matrix.app-service-plan-prod }} \
          -DresourceGroup=brandon-prod \
          -Dregion=eastus2 \
          clean package azure-functions:deploy

  deploy_ui_production:
    name: Deploy UI to Production
    runs-on: ubuntu-latest
    needs:
      - build_ui
      - stage_production_build
    environment:
      name: Production
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: ui-build
          path: ui/dist
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Create UI App Service if it doesn't exist
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            # Check if the app service already exists
            if ! az webapp show --name joaz-ui-9021-prod --resource-group brandon-prod &>/dev/null; then
              echo "Creating UI App Service joaz-ui-9021-prod..."
              az webapp create \
                --name joaz-ui-9021-prod \
                --resource-group brandon-prod \
                --plan asp-joaz-func-helloworld-9021-prod \
                --runtime "NODE|18LTS"
              
              # Configure the webapp for static site hosting
              az webapp config set \
                --name joaz-ui-9021-prod \
                --resource-group brandon-prod \
                --always-on true
              
              # Set the startup command
              az webapp config set \
                --name joaz-ui-9021-prod \
                --resource-group brandon-prod \
                --startup-file "npm install -g serve && serve -s . -p 8080"
            else
              echo "UI App Service joaz-ui-9021-prod already exists, skipping creation."
            fi
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install dependencies
        working-directory: ./ui
        run: npm ci --legacy-peer-deps
      - name: Build UI for Production environment
        working-directory: ./ui
        env:
          VITE_USER_API_URL: https://joaz-func-user-9021-prod.azurewebsites.net/api
          VITE_PRODUCT_API_URL: https://joaz-func-product-9021-prod.azurewebsites.net/api
          VITE_ORDER_API_URL: https://joaz-func-order-9021-prod.azurewebsites.net/api
        run: npm run build
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'joaz-ui-9021-prod'
          package: ./ui/dist
          startup-command: 'npm install -g serve && serve -s . -p 8080'
      - name: Configure App Settings
        uses: azure/CLI@v1
        with:
          azcliversion: latest
          inlineScript: |
            az webapp config appsettings set --name joaz-ui-9021-prod --resource-group brandon-prod --settings \
              VITE_USER_API_URL=https://joaz-func-user-9021-prod.azurewebsites.net/api \
              VITE_PRODUCT_API_URL=https://joaz-func-product-9021-prod.azurewebsites.net/api \
              VITE_ORDER_API_URL=https://joaz-func-order-9021-prod.azurewebsites.net/api

  smoke_test_production:
    name: Smoke Test for Production (${{ matrix.app }})
    needs: 
      - discover-apps
      - deploy_production
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.discover-apps.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Health Check
        run: |
          echo "Running health check for ${{ matrix.app }} in Production environment..."
          
          # Retry logic for health check
          max_attempts=3
          attempt=1
          
          while [ $attempt -le $max_attempts ]; do
            echo "Attempt $attempt of $max_attempts"
            
            # Call health endpoint and check response
            response=$(curl -s -w "\n%{http_code}" "https://${{ matrix.function-app-name-prod }}.azurewebsites.net/api/health")
            http_code=$(echo "$response" | tail -n1)
            body=$(echo "$response" | head -n -1)
            
            echo "HTTP Status: $http_code"
            echo "Response: $body"
            
            # Check if HTTP status is 200 and response contains "UP" status (Spring Boot Actuator format)
            if [ "$http_code" = "200" ] && echo "$body" | grep -q '"status":"UP"'; then
              echo "Health check passed for ${{ matrix.app }} in Production environment"
              exit 0
            else
              echo "Health check failed for ${{ matrix.app }} in Production environment (attempt $attempt)"
              if [ $attempt -lt $max_attempts ]; then
                echo "Retrying in 10 seconds..."
                sleep 10
              fi
            fi
            
            attempt=$((attempt + 1))
          done
          
          echo "Health check failed after $max_attempts attempts"
          exit 1

  notify_prod_release:
    name: Send Notifications of Prod release completed
    needs: deploy_production
    runs-on: ubuntu-latest
    steps:
      - name: Send Production Release Notifications
        run: |
          echo "Sending notifications for Azure Function production release..."
          # Add notification logic here

  create_hotfix_branch:
    name: Create Hotfix Branch
    needs: deploy_production
    runs-on: ubuntu-latest
    steps:
      - name: Create Hotfix Branch
        run: |
          echo "Creating hotfix branch for Azure Function..."
          # Add hotfix branch creation logic here