spring:
  application:
    name: helloworld2-function
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true

logging:
  level:
    com.example: DEBUG
    org.springframework: INFO
    root: INFO

app:
  name: HelloWorld2
  version: v2.2.0
  build-date: 2024-01-15

server:
  port: 8080
