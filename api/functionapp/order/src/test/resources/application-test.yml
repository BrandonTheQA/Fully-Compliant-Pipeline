spring:
  profiles:
    active: test
  
  application:
    name: order-function-test

logging:
  level:
    com.example: DEBUG
    org.springframework: WARN
    root: INFO

# Test-specific configuration
services:
  user:
    base-url: http://localhost:8081/api
  product:
    base-url: http://localhost:8082/api

