apiVersion: apps/v1
kind: Deployment
metadata:
  name: product
  namespace: dev
spec:
  template:
    spec:
      containers:
        - name: product
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "aks"
          resources:
            requests:
              cpu: "200m"
              memory: "384Mi"
            limits:
              cpu: "500m"
              memory: "768Mi"

